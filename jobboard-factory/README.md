# 🏽 JobBoard Factory --- Master Integration Spec

Production-grade TypeScript adapters that transform raw job board pages/APIs into a normalized `ScrapedJob` object.

## Quick Start

```bash
npm install
cp .env.example .env   # fill in France Travail credentials
npm run dev            # starts Express API on :3001
```
